machine:
  node:
    version: 0.10

dependencies:
  pre:
    - export DISPLAY=:99.0
    - sudo /etc/init.d/xvfb start
  override:
    - npm install -g bower

test:
  pre:
    - bower update
    - bower install


