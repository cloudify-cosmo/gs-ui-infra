machine:
  node:
    version: 0.10
  environment:
    DISPLAY: :99.0

dependencies:
  pre:
    - /etc/init.d/xvfb start
  override:
    - npm install -g bower

test:
  pre:
    - bower update
    - bower install


