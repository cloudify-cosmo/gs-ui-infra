machine:
  node:
    version: 0.10.40
  environment:
    DISPLAY: :99.0

dependencies:
  override:
    - npm install -g bower
    - /etc/init.d/xvfb start

test:
  pre:
    - bower update
    - bower install


